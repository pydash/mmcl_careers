Things to do:
Change .env.template to .env
```bash
cp .env.template .env
```
Then place the actual values



## Getting Started

First, run the development server:

```bash
npm run dev # most likely to run
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.